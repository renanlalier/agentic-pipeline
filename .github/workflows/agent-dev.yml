name: agent-dev (reusable)

# Passo 4 do fluxo. Roda DENTRO do repo de produto.
# github.repository aqui e o repo consumidor - so o ARQUIVO veio da platform.
# Cada um dos N repos executa este workflow em VM propria, sem ver os outros.
#
# Duas fases do MESMO papel, dois runs do agente:
#   fase 1 - detalhar: usa a skill writing-plans para mapear arquivos e
#            publicar um plano na sub-issue ANTES de tocar em codigo.
#   fase 2 - implementar: usa o plano da fase 1 como contexto adicional.

on:
  workflow_call:
    inputs:
      role:
        required: true
        type: string
      execution_id:
        required: true
        type: string
      sub_issue:
        required: true
        type: number
      contract_ref:
        required: false
        type: string
        default: "v1"
      cli_override:
        required: false
        type: string
        default: ""
      model_override:
        required: false
        type: string
        default: ""
    secrets:
      PIPE_TOKEN:
        required: false

jobs:
  implementar:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout deste repositorio
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Checkout da platform
        uses: actions/checkout@v5
        with:
          repository: renanlalier/poc-agentic-platform
          ref: v1
          path: .platform

      - name: Instalar yq
        run: |
          sudo wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Provar o isolamento
        run: |
          echo "### Isolamento deste run" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- repositorio: \`${{ github.repository }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- run id: \`${{ github.run_id }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- execution id: \`${{ inputs.execution_id }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- runner: \`$(hostname)\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Conteudo do workspace (so este repo esta em disco):" >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
          ls -1 | grep -v '^\.platform$' >> "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"

      - name: Verificar limites do repo
        run: |
          set -euo pipefail
          if [ -f .agentic/config.yml ]; then
            echo "Caminhos proibidos para o agente neste repo:"
            yq -r '.constraints.forbid_paths[]?' .agentic/config.yml || true
          fi

      - name: Ler a sub-issue
        id: subissue
        env:
          GH_TOKEN: ${{ secrets.PIPE_TOKEN || secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          SUB_ISSUE: ${{ inputs.sub_issue }}
        run: |
          set -euo pipefail
          gh api "repos/$REPO/issues/$SUB_ISSUE" > subissue.json
          jq -r '.title + "\n" + (.body // "")' subissue.json > subissue.txt

      - name: "Fase 1 - Detalhar (skill writing-plans)"
        id: plano
        uses: ./.platform/actions/run-agent
        with:
          role: ${{ inputs.role }}
          cli_override: ${{ inputs.cli_override }}
          model_override: ${{ inputs.model_override }}
          execution_id: ${{ inputs.execution_id }}
          prompt: "Use a skill writing-plans. Antes de qualquer edicao, mapeie os arquivos deste repositorio que a sub-issue #${{ inputs.sub_issue }} vai tocar e escreva um plano de tarefas pequenas, com caminho de arquivo exato e criterio de verificacao. Nao implemente ainda - so publique o plano. Sub-issue:\n\n$(cat subissue.txt)"
          platform_path: .platform

      - name: Publicar o plano na sub-issue (REST)
        env:
          GH_TOKEN: ${{ secrets.PIPE_TOKEN || secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          SUB_ISSUE: ${{ inputs.sub_issue }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail
          cat <<'PLANOEOF' > plano_result.json
          ${{ steps.plano.outputs.result }}
          PLANOEOF

          # o adapter dry-run nao produz um plano de verdade (sem modelo);
          # em produção (cursor/codex) o campo summary/notes carrega o
          # plano detalhado. Publicamos o que veio, sempre.
          PLANO_TEXTO=$(jq -r '(.summary // "") + "\n\n" + (.notes // "")' plano_result.json)

          {
            echo "## Plano de implementação (fase de detalhamento)"
            echo ""
            echo "$PLANO_TEXTO"
            echo ""
            echo "---"
            echo "_run: $RUN_URL_"
          } > plano_comment.md
          jq -Rs '{body: .}' plano_comment.md > plano_payload.json
          gh api "repos/$REPO/issues/$SUB_ISSUE/comments" --input plano_payload.json

          # arquivo local para a fase de implementacao usar como contexto extra
          cp plano_comment.md plano_para_implementacao.md

      - name: Criar branch de trabalho
        env:
          EXEC_ID: ${{ inputs.execution_id }}
        run: |
          git config user.name  "agentic-${{ inputs.role }}"
          git config user.email "agentic@users.noreply.github.com"
          git checkout -b "agentic/$EXEC_ID"

      - name: "Fase 2 - Executar o papel (implementar)"
        id: agent
        uses: ./.platform/actions/run-agent
        with:
          role: ${{ inputs.role }}
          cli_override: ${{ inputs.cli_override }}
          model_override: ${{ inputs.model_override }}
          execution_id: ${{ inputs.execution_id }}
          prompt: "Implemente a sub-issue #${{ inputs.sub_issue }} deste repositorio, respeitando o contrato ${{ inputs.contract_ref }} e o plano que voce publicou na fase de detalhamento:\n\n$(cat plano_para_implementacao.md)"
          platform_path: .platform

      - name: Materializar a alteracao
        env:
          EXEC_ID: ${{ inputs.execution_id }}
          ROLE: ${{ inputs.role }}
          CLI: ${{ steps.agent.outputs.cli }}
          MODEL: ${{ steps.agent.outputs.model }}
          SUB_ISSUE: ${{ inputs.sub_issue }}
          CONTRACT_REF: ${{ inputs.contract_ref }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail
          {
            echo "## $EXEC_ID"
            echo ""
            echo "- papel: \`$ROLE\`"
            echo "- cli: \`$CLI\`"
            echo "- modelo: \`$MODEL\`"
            echo "- sub-issue: #$SUB_ISSUE"
            echo "- contrato: \`$CONTRACT_REF\`"
            echo "- run: $RUN_URL"
            echo ""
          } >> CHANGELOG-agentic.md

          git add CHANGELOG-agentic.md
          git commit -m "feat: $ROLE para #$SUB_ISSUE (exec $EXEC_ID)"
          git push origin "agentic/$EXEC_ID"

      - name: Abrir pull request em draft (REST)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          EXEC_ID: ${{ inputs.execution_id }}
          ROLE: ${{ inputs.role }}
          CLI: ${{ steps.agent.outputs.cli }}
          MODEL: ${{ steps.agent.outputs.model }}
          SUB_ISSUE: ${{ inputs.sub_issue }}
          CONTRACT_REF: ${{ inputs.contract_ref }}
        run: |
          set -euo pipefail
          {
            echo "Gerado pela pipe agentica."
            echo ""
            echo "| campo | valor |"
            echo "|---|---|"
            echo "| execution_id | \`$EXEC_ID\` |"
            echo "| papel | \`$ROLE\` |"
            echo "| cli | \`$CLI\` |"
            echo "| modelo | \`$MODEL\` |"
            echo "| contrato | \`$CONTRACT_REF\` |"
            echo ""
            echo "Closes #$SUB_ISSUE"
            echo ""
            echo "---"
            echo "**HITL 2** — este PR precisa de review humano. O App que abriu nao pode aprovar."
          } > pr_body.md

          jq -n \
            --arg title "[agentic] $ROLE — sub-issue #$SUB_ISSUE" \
            --rawfile body pr_body.md \
            --arg head "agentic/$EXEC_ID" \
            --arg base "main" \
            '{title: $title, body: $body, head: $head, base: $base, draft: true}' \
            > pr_payload.json

          gh api "repos/$REPO/pulls" --input pr_payload.json > pr.json
          echo "PR criado: $(jq -r .html_url pr.json)"

      - name: Reportar na sub-issue (REST)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          SUB_ISSUE: ${{ inputs.sub_issue }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail
          MSG="Implementacao concluida em \`$REPO\`. Run: $RUN_URL"
          jq -n --arg body "$MSG" '{body: $body}' > comment_payload.json
          gh api "repos/$REPO/issues/$SUB_ISSUE/comments" --input comment_payload.json
